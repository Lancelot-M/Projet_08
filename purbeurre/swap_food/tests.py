from django.test import TestCase
from django.urls import reverse
from .models import Aliments

#Faire l'arbre des catégories .
    TAGS INTERESSANTS dans products: [{TAGS: ...},{TAGS: ...},]
#Récupérer toutes les catégories en dernière génération.
#Filtrer les cat qui n'ont pas mini 6 produits avec nom, photo, notre, valeur nutri.
#Récupérer les produits fr contenant (nom, note, valeur nutri, url, category) depuis off.
#travail sur la casse ?
#mise en place mvt