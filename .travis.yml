language: python
python:
    - "3.8"
addons:
    chrome: stable
before-install:
    - "export DISPLAY=99:0"
    - "sh -e /etc/init.d/xvfb start"
    - sleep 3
install:
    - pip install pipenv
    - pipenv install

env: 
    - DJANGO_SETTINGS_MODULE="settings.travis"

services:
    - postgresql
  
before_script:
        - cd purbeurre/
script:
        - pytest
